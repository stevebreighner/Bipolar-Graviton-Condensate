# Graviton Paper Layout Tree

This tree app maps the paper sequence as a single trunk:

- graviton status first
- transfer framing second
- three main routes:
  - massless + conventional framing
  - massless + transfer field
  - massive/extended + transfer field

The app supports pan, zoom, export, and local JSON save/load using the same scaffold as the main theory tree.
